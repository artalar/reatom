# reatom-jsx-tree

Checkbox tree with tri-state (checked / unchecked / indeterminate) nodes — port of the [v3 react-tree](https://github.com/reatom/reatom/tree/v3/examples/react-tree) example to `@reatom/core` v1001 and `@reatom/jsx`.

```bash
pnpm install
pnpm dev
```
