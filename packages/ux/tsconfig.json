{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "declarationMap": true,
    "lib": ["DOM", "DOM.Iterable", "ESNext"],
    "types": ["vitest", "node"]
  },
  "include": ["src", "spike", "tools"],
  "exclude": ["build", "dist"]
}
