{
  "extends": "../../tsconfig.build.json",
  "compilerOptions": {
    "declarationMap": true,
    "lib": ["DOM", "DOM.Iterable", "ESNext"],
    "types": ["vitest", "@vitest/browser-playwright"]
  }
}
