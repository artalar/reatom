{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "declarationMap": true,
    "lib": ["DOM", "DOM.Iterable", "ESNext"],
    "paths": {
      "test": ["./src/test.ts"]
    },
    "types": ["vitest", "@vitest/browser-playwright"]
  },
  "exclude": ["build", "dist"]
}
